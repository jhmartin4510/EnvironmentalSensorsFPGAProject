# Applying Digital Signals with I2C

When working with digital sensors that have register addresses for storing sensor readings, I2C is one way these devices communciate with other hardware to send and receive information. Since these projects are intended for reading sensor data, the state machine focuses on reading from the register on the sensor device that stores the sensor data. 

